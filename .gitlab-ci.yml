image: docker-registry.suggest.io/sio/os-www:latest

stages:
  - build
  - deploy

build-www-docker:
  stage: build
  before_script:
  - sbt clean
  script:
  - sbt 'project www' docker:publishLocal

