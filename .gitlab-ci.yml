image: docker-registry.suggest.io:5000/sio/sio2/os-www-build:latest

stages:
  - build
  #- deploy

#cache:
#  paths:
#    - src1/server/www/target/universal

## Полная сборка www-релиза:
www-stage:
  stage: build
  # TODO cache:
  #- project/target
  #- project/project
  before_script:
  - /sbin/entrypoint.sh
  script:
  - sbt 'project www' stage
  - src1/server/docker/scripts/deploy-www-stage-to-nodes.sh  src1/server/www  sio2/sioweb21  sio2.suggest.io sio3.suggest.io

## Развёртывание на серверах. (надо разобраться, почему target/universal недоступен отсюда).
#www-stage-deploy-nodes:
#  stage: deploy
#  before_script:
#  - /sbin/entrypoint.sh
#  script:
#  - src1/server/docker/scripts/deploy-www-stage-to-nodes.sh  src1/server/www  sio2/sioweb21  sio2.suggest.io sio3.suggest.io sio5.suggest.io
