image: docker-registry.suggest.io/sio/sio2/os-www-build:latest

stages:
  - build
  - deploy

before_script:
  - /sbin/entrypoint.sh
  - sbt clean

build-www-docker:
  stage: build
  script:
  - sbt 'project www' docker:publishLocal

