common
-----------------------

Набор констант и каркасов для enum-моделей, общих интерфейсов для кросс-компиляцию
и в scala.js, и в web (play).

Позволяет избегать логических ошибок в общении между клиентом и сервером.

! У проекта common НЕ должно быть зависимостей, кроме кросс-компилируемых в подчиненных sjs-проектах.

