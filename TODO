-- В браузере при листании выдачи должна работать браузерная кнопка "назад".
-- появляющиеся стрелки о прокрутке в право в лево карточек.
-- вижу что люди столкнулись с не пониманием что зайдя на карточку можно листать в право и в лево как-то это надо подсветить
-- в редакторах блоков, при инициализации colorpicker передавать текущий цвет

-- В отдельной ветке (например с названием "blocks-unity"):
   Нужно унифицировать формат блоков до шести текстовых полей + остальное.
   Блок за блоком, пошагово.
     В коде BlocksConf:
       Нужно заменять аддоны полей Discount, Price, OldPrice, Title, Descr на TitleDescrListBlockT
       Ошибки типа "бла-бла-бла overrides nothing" резолвить через удаление куска кода, к которому относится ошибка.
     В вёрстке заменять код типа "mad.offers.headOption.map" на "mad.offers.map". Т.е. удалять вызов headOption() для списка mad.offers.
     Ненужные поля, ненужную верстку и т.д. срезать начисто.
     Все блоки, у которых в BlocksConf isShown = false (КРОМЕ block26) удалить вместе со всей вёрсткой.

-- MInviteRequest: впилить поддержку установки и хранения встроенных экземпляров billing-моделей.
-- Карточка приветствия MWelcomeAd: унифицировать с MAd.
-- Платное размещение карточки приветствия (нужно сначала MWelcomeAd -> MAd).
-- Рекламное посредничество: сторонний узел вешает и прозрачно получает комиссию с размещения на конкретных узлах-приёмниках.
-- Выпилить leader/slave-барахло из шаблонов.
-- advForm: поддержка unbind для формы. Сейчас при ошибке форма сбрасывается. Для этого надо рендерить таблицу через
   list-маппинги формы, а не через данные моделей, передаваемые из аргументов.
-- Поддержка парсинга и сохранения платежных реквизитов в отдельной sql-модели.
-- Постмодерация карточкек админами suggest.io: уведомлять продьюсеров карточек о сокрытии оных.
-- Маппинг для шрифтов в редакторе блоков. Сейчас шрифты захардкожены, их названия не проверяются вообще. Нужен маппер и Enumeration с названиями.
-- Поиск по слову "кофе" выдаёт кучу хлама.
