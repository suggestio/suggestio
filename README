extadv-sjs-runner

Для размещения в соц.сетях используется js-api соц.сетей.
Для взаимодействия с js-api используется прослойка, расписанная
в настоящем проекте.

Это первая версия js-подсистемы внешнего размещения в соц.сетях,
которая написана на scala-js. Но вторая версия после оригинального SioPR.


----
scala.js 0.6.0:

scala.Enumeration + fullOptJS	    => +52 KiB (достоверно)
scala.collections		    => ~ +80 KiB (наверное примерно)

s"" string interpolator без уже подключенных коллекций => +24 KiB (достоверно).
  С уже включенными коллекциями и StringBuilder'ами -- +400 байт.
