@(args: blk.CssRenderArgsT)

@* Стили для блока в общем. *@
@import args._

@defining( cssClasses.map {c => List(".", c, " ")} ) { cc =>
.sm-block@{mad.id}@{cc} {
  width: @((mad.blockMeta.width * szMult).toInt)px;
  height: @((mad.blockMeta.height * szMult).toInt)px;
}
}